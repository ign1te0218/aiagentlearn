(function () {
  const q = (prompt, options, answer, explanation) => ({ prompt, options, answer, explanation });

  const quizzes = {
    'start-here': [
      q('在这条转型路线中，三门语言的职责如何划分最合理？', ['TypeScript/Node.js 负责主应用，Python 负责 AI 生态辅助能力', '三门语言都要达到框架源码级深度', '只学 Python，前端与服务端继续交给低代码平台'], 0, '路线以 TypeScript/Node.js 为主，Python 达到能读、能改、能写辅助服务的程度。'),
      q('为什么先补基础，再进入 Agent 框架？', ['框架会自动修复所有基础错误', '先理解类型、异步、网络和数据边界，才能判断框架行为与故障', '基础知识只在面试中有用'], 1, 'Agent 框架仍建立在语言、运行时、网络和数据系统之上，基础薄弱会让问题无法定位。'),
      q('哪项证据最能说明一章已经真正完成？', ['看完教程并收藏链接', '代码可运行、能预测修改后的结果，并记录错误与解释', '复制示例后没有出现红色报错'], 1, '完成要求包含运行、预测、解释和可复现记录，而不只是阅读或复制。')
    ],
    'ts-setup-tsconfig': [
      q('TypeScript 源码进入 Node.js 常规生产运行链路前，通常需要什么步骤？', ['由 `tsc` 或构建工具检查并生成 JavaScript', '浏览器自动把类型写入数据库', '只修改文件扩展名为 `.js`'], 0, '常规链路是类型检查/编译后运行 JavaScript；Node.js 类型剥离能力也不能替代完整工程检查。'),
      q('为什么基础项目应开启 `strict`？', ['让所有变量自动变成 `any`', '尽早暴露空值、隐式 any 和不安全赋值等边界问题', '让生成的 JavaScript 一定更快'], 1, '`strict` 的价值是扩大静态检查覆盖面，不是运行时性能优化。'),
      q('下列哪项属于运行时错误而不是编译期类型错误？', ['给 `number` 变量赋字符串', '访问真实响应中不存在的字段导致异常', '函数声明返回 `string` 却返回数字'], 1, '外部数据与真实执行行为发生在运行时，类型声明本身不能保证响应真实形状。')
    ],
    'ts-primitives-collections': [
      q('什么时候更值得显式写类型注解？', ['初始化值已经能准确推断时每处都重复注解', '公共函数边界或空数组无法表达预期元素类型时', '为了关闭 strict 检查时'], 1, '局部明显值可依赖推断，公共边界和信息不足的位置需要明确注解。'),
      q('元组比普通数组更适合表达哪种数据？', ['任意长度的同类型日志列表', '位置和长度具有固定业务含义的坐标', '未知结构的接口响应'], 1, '元组用于固定长度且每个位置含义明确的数据。'),
      q('收到类型为 `unknown` 的值后，正确做法是什么？', ['直接按目标对象访问字段', '先通过类型守卫或 schema 校验再使用', '把它断言为 `any` 并忽略错误'], 1, '`unknown` 强制调用方先验证，这是外部数据边界更安全的默认选择。')
    ],
    'ts-object-modeling': [
      q('有限业务状态为什么应优先使用字面量联合？', ['可以阻止拼写错误和未定义状态进入模型', '会让运行时自动生成状态机', '可以省略所有分支处理'], 0, '字面量联合把允许值限制在明确集合中。'),
      q('哪种场景通常更适合 `type`？', ['需要声明可扩展对象契约', '需要表达联合、交叉或条件组合', '需要生成运行时校验器'], 1, '`type` 擅长联合与组合；interface 常用于可扩展对象契约。两者都不会自动生成运行时校验。'),
      q('可辨识联合解决了什么问题？', ['让所有字段都变成可选', '通过共同判别字段把状态和该状态合法字段绑定', '让对象可以存储任意属性'], 1, '判别字段让控制流收窄到具体分支，并减少非法字段组合。')
    ],
    'ts-functions-async': [
      q('函数类型边界中最重要的两部分是什么？', ['变量名和文件名', '参数类型和返回值类型', '注释数量和行数'], 1, '参数与返回值定义调用方和实现方之间的契约。'),
      q('一个标记为 `async` 的函数返回 `number` 值时，调用方实际得到什么？', ['`number`', '`Promise<number>`', '`unknown`'], 1, '`async` 函数始终返回 Promise，普通返回值会被包装。'),
      q('如何避免异步错误被静默丢弃？', ['调用 Promise 后不保存结果', '在明确边界 `await`/返回 Promise，并集中捕获或传播错误', '给所有回调增加空的 catch'], 1, '异步操作必须进入可观察的 Promise 链，并在合适边界处理；空 catch 会吞掉故障。')
    ],
    'ts-narrowing-exhaustive': [
      q('哪种操作能让 TypeScript 在分支内可靠收窄联合类型？', ['只修改变量名', '使用 `typeof`、`in` 或判别字段检查', '把值转换为字符串'], 1, '控制流检查提供了可证明的类型信息。'),
      q('自定义类型守卫最危险的错误是什么？', ['函数名太长', '返回 true 却没有真正检查声明所需字段', '使用了布尔返回值'], 1, '不诚实的守卫会欺骗编译器，让非法运行时数据被当作合法类型。'),
      q('在 switch 的 default 分支把值赋给 `never` 有什么作用？', ['自动忽略未知状态', '新增联合成员后让遗漏分支产生编译错误', '让 default 永远在运行时执行'], 1, '`never` 穷尽检查把未处理的新状态转为编译期反馈。')
    ],
    'ts-generics-keyof': [
      q('泛型的核心价值是什么？', ['把所有类型替换为 any', '在输入、输出或字段之间保留真实类型关系', '减少所有运行时代码'], 1, '泛型表达可复用逻辑中的类型关系，而不是放弃类型信息。'),
      q('`T extends { id: string }` 表达什么？', ['T 只能恰好有 id 一个字段', 'T 至少具备字符串 id，仍可包含其他字段', 'T 是运行时继承的类'], 1, '泛型约束描述最低能力，不限制额外属性。'),
      q('`K extends keyof T` 与 `T[K]` 组合能保证什么？', ['K 必须是 T 的合法键，返回类型与该字段一致', 'K 可以是任意字符串', '所有字段返回 string'], 0, 'keyof 和索引访问类型共同维持键与字段值类型的对应关系。')
    ],
    'ts-utility-modules-errors': [
      q('需要从已有 User 模型中只取 `id` 和 `name`，哪项最合适？', ['`Pick<User, "id" | "name">`', '`Partial<string>`', '`Record<User, never>`'], 0, '`Pick` 从现有对象类型选择指定字段。'),
      q('清晰的 ESM 模块边界应该具备什么特征？', ['所有文件互相导入内部实现', '通过公共入口导出稳定能力，依赖方向明确', '依赖全放在全局变量中'], 1, '模块边界应限制内部细节泄漏并保持清晰依赖方向。'),
      q('为什么业务错误需要稳定错误代码？', ['错误消息永远不能变化', '调用方可按代码处理，日志和 UI 不依赖易变文案', '错误代码会自动重试请求'], 1, '稳定代码是机器可依赖的契约，文案可面向用户调整。')
    ],
    'ts-runtime-validation-client': [
      q('为什么 `response.json() as User` 不足以保证安全？', ['断言只影响编译器，不检查真实 JSON', 'JSON 不支持对象', 'User 类型会被发送给服务器'], 0, 'TypeScript 类型在运行时被擦除，外部数据需要 schema 或守卫验证。'),
      q('需要自行处理校验失败并返回统一错误时，应优先使用什么？', ['`safeParse` 并检查结果', '无条件类型断言', '关闭 strict'], 0, '`safeParse` 把成功与失败显式建模，适合错误转换。'),
      q('可靠 API Client 的最小边界不包括哪项？', ['超时与取消', 'HTTP/业务错误分类', '相信所有 200 响应都符合类型声明'], 2, '即使状态码成功，响应体仍必须验证。')
    ],
    'node-runtime-project': [
      q('Node.js 相比浏览器主要额外提供哪类能力？', ['DOM 布局引擎', '文件、进程、网络和服务端运行时 API', '自动训练大模型'], 1, 'Node.js 面向服务端与命令行环境，不提供浏览器 DOM。'),
      q('REPL 最适合做什么？', ['长期维护完整应用', '快速验证表达式和小段 API 行为', '替代自动化测试'], 1, 'REPL 适合快速实验，正式代码仍应保存到文件并测试。'),
      q('为什么应用项目通常应提交 lockfile？', ['记录可复现的精确依赖解析结果', '把 node_modules 上传到 Git', '隐藏所有依赖版本'], 0, 'lockfile 降低不同环境解析到不同依赖树的风险。')
    ],
    'node-modules-esm-cjs': [
      q('在 `.js` 文件中，哪项会影响 Node.js 按 ESM 还是 CommonJS 解释？', ['package.json 的 `type` 字段', 'CPU 核心数量', '文件内容行数'], 0, '`type: module` 会让对应包范围内的 `.js` 按 ESM 处理。'),
      q('模块缓存与顶层副作用组合可能造成什么问题？', ['导入次数一定等于副作用执行次数', '首次导入执行副作用，后续复用缓存，行为隐藏且难测试', '缓存会删除所有导出'], 1, '顶层副作用和缓存会让初始化时机隐蔽，应尽量显式调用。'),
      q('ESM 中获取当前模块文件路径应使用什么基础？', ['`import.meta.url` 并按需配合 `fileURLToPath`', '浏览器的 `document.location`', '固定写死 C 盘路径'], 0, 'ESM 使用 import.meta.url 表示模块 URL，再转换为本地路径。')
    ],
    'node-event-loop-deep-dive': [
      q('Node.js 24 顶层微任务实验中，哪项描述正确？', ['CJS 与 ESM 永远都是 Promise 先于 nextTick', 'CJS 通常 nextTick 先于 Promise，ESM 顶层通常相反', '顺序只由变量名决定'], 1, 'ESM 顶层求值上下文会影响微任务顺序，不能背一个全局固定规则。'),
      q('在同一个 I/O 回调中安排 `setImmediate` 和 `setTimeout(0)`，通常谁先执行？', ['setImmediate', 'setTimeout(0)', '必须同时执行'], 0, 'poll 回调结束后通常先进入 check 阶段执行 setImmediate。'),
      q('把 3 秒 CPU 循环放进 Promise 能避免阻塞事件循环吗？', ['能，Promise 会自动创建线程', '不能，同步 CPU 工作仍占用主线程', '只在 Windows 上能'], 1, 'Promise 不会自动把同步计算移到其他线程。')
    ],
    'node-async-errors-cancel': [
      q('处理 1000 个外部请求时，为什么不应直接无限 `Promise.all`？', ['Promise.all 不支持结果', '可能压垮连接、内存或上游限额，应限制并发', '它会强制串行'], 1, '受限并发在吞吐和资源边界之间取得平衡。'),
      q('异步错误应在哪里处理？', ['每层都 catch 后忽略', '能恢复的层处理，无法恢复时传播到统一边界', '只写 console.log'], 1, '错误处理应有明确职责，不能静默吞掉。'),
      q('可靠取消链路的关键是什么？', ['只修改按钮文字', '同一个或组合后的 AbortSignal 沿调用链传递并清理资源', '等待请求自然结束'], 1, '取消协议必须到达实际 I/O，并在 finally 等位置释放资源。')
    ],
    'node-event-emitter': [
      q('`EventEmitter.emit()` 默认如何调用监听器？', ['同步按注册顺序调用', '始终在新线程调用', '随机异步调用'], 0, 'emit 默认同步执行监听器，耗时监听器也会阻塞当前流程。'),
      q('EventEmitter 发出 `error` 事件但没有监听器时会怎样？', ['自动忽略', '通常抛出错误并可能终止进程', '自动转换成 warning 后继续'], 1, '`error` 是特殊事件，需要明确监听。'),
      q('如何降低长期对象的监听器泄漏风险？', ['每次调用都重复 on 且从不清理', '使用 once 或在生命周期结束时 off/removeListener', '提高最大监听器警告阈值后不处理'], 1, '监听器注册与清理必须成对管理。')
    ],
    'node-files-path': [
      q('读取文本文件时为什么要明确 `utf8`？', ['否则默认得到 Buffer，直接当文本可能产生错误理解', 'utf8 会自动加密文件', '可以让磁盘容量翻倍'], 0, '编码决定字节如何解释为字符。'),
      q('校验用户提供的相对路径时，哪种做法更安全？', ['字符串包含 `..` 就一律通过', 'resolve 后检查最终路径仍位于允许根目录内', '直接拼接根目录和输入'], 1, '安全判断应基于规范化后的最终路径归属。'),
      q('关键配置文件如何减少部分写入风险？', ['直接覆盖并在中途终止进程', '先写临时文件，完成后原子替换', '每写一行暂停一秒'], 1, '临时文件加替换避免读到半成品。')
    ],
    'node-buffer-encoding': [
      q('为什么字符串长度不等于 UTF-8 字节长度？', ['一个字符可能由多个 UTF-8 字节表示', 'Buffer 会删除中文', '字符串只支持 ASCII'], 0, '字符、UTF-16 代码单元和 UTF-8 字节是不同层次。'),
      q('处理未知内容时，为什么优先 `Buffer.alloc` 而不是 `allocUnsafe`？', ['alloc 会初始化内存，避免泄漏旧数据', 'allocUnsafe 不能分配内存', 'alloc 会自动压缩图片'], 0, 'allocUnsafe 需要调用方确保完全覆盖，否则可能暴露旧内存。'),
      q('中文字符被拆在两个 chunk 中时应如何解码？', ['每块独立 toString 后直接拼接', '使用 StringDecoder 或流的 setEncoding', '删除不完整字节'], 1, '解码器会保留未完成的多字节序列，等待后续 chunk。')
    ],
    'node-stream-backpressure': [
      q('哪种场景最适合使用 Stream？', ['一次读取超大文件并逐块处理', '保存三个布尔值', '计算两个数字之和'], 0, '流避免把完整大数据一次性放入内存。'),
      q('Writable 的 `write()` 返回 false 表示什么？', ['写入永久失败', '生产者应暂停，等待 drain 后继续', '数据已全部落盘'], 1, 'false 是背压信号，表示内部缓冲已达到阈值。'),
      q('为什么优先使用 `pipeline()` 连接多个流？', ['统一传播错误、完成和销毁', '让所有流变成同步', '绕过背压'], 0, 'pipeline 管理整条链路的错误与资源清理，并保留背压。')
    ],
    'node-http-capstone': [
      q('读取 JSON 请求体时为什么必须限制大小？', ['防止无界内存占用和拒绝服务风险', '让 JSON 自动变成 XML', '限制会提升所有请求速度'], 0, '请求体是外部输入，必须有明确资源上限。'),
      q('静态文件路由最重要的路径安全检查是什么？', ['文件名必须是英文', '解析后的路径不能逃出静态根目录', '路径长度必须是偶数'], 1, '规范化路径归属可阻止目录穿越。'),
      q('优雅关闭服务通常要做什么？', ['立即强制结束所有连接', '停止接收新请求，等待在途请求并设置最终超时', '只删除日志'], 1, '优雅关闭需要兼顾新流量入口、在途工作和退出上限。')
    ],
    'python-environment-project': [
      q('本课程要求的 Python 最低版本是什么？', ['3.8', '3.10', '3.12'], 2, '课程统一使用 Python 3.12+。'),
      q('虚拟环境的主要作用是什么？', ['让每个项目隔离解释器入口和依赖', '把源码自动上传', '替代 Git'], 0, 'venv 隔离项目依赖，减少全局环境冲突。'),
      q('为什么推荐 `python -m pip`？', ['明确使用当前 Python 解释器对应的 pip', '可以跳过网络', '会自动生成 Dockerfile'], 0, '该写法减少 pip 与 python 指向不同环境的风险。')
    ],
    'python-syntax-control': [
      q('为什么判断可选数字时不应简单写 `if value`？', ['0 会被当作假值，可能与 None 混淆', 'Python 不支持 if', '数字永远为真'], 0, '业务上 0 可能是合法值，需要按语义显式判断 None。'),
      q('遍历列表元素并同时需要序号时，优先使用什么？', ['手工维护索引变量', '`enumerate(items)`', '把列表转换为字符串'], 1, 'enumerate 直接表达“索引加元素”的意图。'),
      q('Python 动态类型意味着什么？', ['对象没有类型', '变量绑定可变化，但对象始终有具体运行时类型', '所有错误都会在编译期发现'], 1, '动态类型改变检查时机，不代表没有类型。')
    ],
    'python-collections-comprehensions': [
      q('需要唯一成员且关注集合运算时应选择什么？', ['list', 'set', 'tuple'], 1, 'set 表达无重复成员并支持交并差。'),
      q('对嵌套列表做浅复制后修改内部列表，为什么副本也可能变化？', ['浅复制仍共享内部可变对象引用', 'Python 会随机同步列表', 'tuple 自动修改 list'], 0, '浅复制只复制最外层容器。'),
      q('什么时候不适合使用推导式？', ['单一映射或过滤', '包含多层复杂分支和副作用', '创建简单平方列表'], 1, '复杂控制流应展开为普通循环以保持可读和可调试。')
    ],
    'python-functions-typing': [
      q('为什么函数的可变默认参数容易出错？', ['默认对象在函数定义时创建并被多次调用共享', '列表不能作为参数', '类型提示会删除列表'], 0, '普通函数使用 None 哨兵并在函数体内创建新对象；dataclass 字段才使用 field(default_factory=...)。'),
      q('LEGB 中函数内部优先查找哪一层？', ['Built-in', 'Global', 'Local'], 2, '名称解析从 Local 开始，再到 Enclosing、Global、Built-in。'),
      q('Python 类型提示会自动拒绝错误运行时输入吗？', ['会，解释器总会强制检查', '不会，需要类型检查器或运行时校验', '只对字符串会'], 1, '类型提示主要供工具和读者使用，不自动成为运行时验证。')
    ],
    'python-modules-exceptions': [
      q('运行包内模块时，哪种方式更能保持导入语义一致？', ['从任意目录直接运行深层文件', '`python -m package.module`', '复制模块到项目根目录'], 1, '-m 按包上下文执行模块。'),
      q('异常处理为什么应捕获具体异常？', ['避免把编程错误和未知故障一起吞掉', '具体异常不会记录日志', '可以关闭测试'], 0, '只捕获能处理的异常，其他错误应继续传播。'),
      q('`raise DomainError(...) from error` 的价值是什么？', ['删除底层原因', '保留异常链，同时转换为业务层错误', '让异常不再抛出'], 1, '异常链既提供稳定边界，也保留根因用于诊断。')
    ],
    'python-files-json': [
      q('`pathlib.Path` 的主要优势是什么？', ['用对象组合跨平台路径操作', '只支持 Linux', '自动上传文件'], 0, 'Path 避免手工拼接分隔符，并提供清晰文件操作 API。'),
      q('使用 `with open(...)` 的核心收益是什么？', ['作用域结束时可靠关闭资源', '文件内容自动加密', '跳过所有 I/O 错误'], 0, '上下文管理器负责资源进入与退出。'),
      q('JSONL 与普通 JSON 数组的主要区别是什么？', ['JSONL 每行一个独立 JSON 值，适合流式处理', 'JSONL 不能存对象', '二者完全相同'], 0, '逐行格式便于追加和处理局部坏行。')
    ],
    'python-classes-dataclasses': [
      q('数据载体主要包含字段和简单行为时，优先考虑什么？', ['dataclass', '全局字典且无约束', '元类'], 0, 'dataclass 可减少样板代码并明确字段。'),
      q('dataclass 中列表字段应如何定义？', ['`items: list = []`', '`field(default_factory=list)`', '把列表放到类外全局'], 1, 'default_factory 为每个实例创建独立列表。'),
      q('构造函数依赖注入为什么有利于测试？', ['测试时可以替换真实依赖为假实现', '会自动生成所有测试', '让依赖变成全局变量'], 0, '显式依赖边界让替换与隔离更容易。')
    ],
    'python-async-fastapi-capstone': [
      q('Python 协程通常在什么时候把控制权交还事件循环？', ['执行任意赋值时', '等待可挂起异步操作的 `await` 时', '定义函数时'], 1, '只有实际可挂起的 await 等位置才会让其他任务运行。'),
      q('CPU 密集同步函数放进 `async def` 会自动并行吗？', ['会', '不会，仍可能阻塞事件循环', '只在 FastAPI 中会'], 1, 'async 主要解决 I/O 等待，不会自动迁移 CPU 工作。'),
      q('FastAPI 请求模型为什么使用 Pydantic？', ['在 HTTP 边界解析并校验外部输入', '替代数据库事务', '隐藏所有接口文档'], 0, 'Pydantic 把外部数据验证转为明确模型与错误响应。')
    ],
    'backend-sql-modeling': [
      q('关系模型中的主键和外键主要表达什么？', ['实体唯一性与实体之间的引用约束', '页面颜色', '缓存过期时间'], 0, '键与约束把业务关系落实到数据库。'),
      q('查询“所有用户以及他们可能为零的会话数”通常需要什么连接？', ['INNER JOIN', 'LEFT JOIN', 'CROSS JOIN 且无条件'], 1, 'LEFT JOIN 保留没有匹配会话的用户。'),
      q('为什么不能把用户输入直接拼进 SQL？', ['会增加 SQL 注入风险，应使用参数化查询', '参数化查询不支持字符串', '拼接只影响格式'], 0, '参数化让 SQL 结构与外部值分离。')
    ],
    'backend-transactions-migrations': [
      q('转账的扣款和入账为什么应放在同一事务？', ['保证共同成功或共同回滚', '让两条 SQL 随机执行', '减少字段数量'], 0, '事务保护跨步骤不变量，避免只完成一半。'),
      q('创建索引的正确依据是什么？', ['所有列都创建索引', '根据真实查询过滤、连接和排序模式评估', '只按列名长度'], 1, '索引提升读取但增加写入和存储成本，应由查询驱动。'),
      q('迁移文件的主要职责是什么？', ['记录可审查、可重复执行的结构版本变化', '保存用户密码', '替代数据备份'], 0, '迁移让数据库结构演进可以追踪和部署。')
    ],
    'backend-redis-cache': [
      q('cache-aside 读取未命中时的常见顺序是什么？', ['返回空值，不访问数据源', '读取权威数据源，再写入带 TTL 的缓存', '把缓存当作唯一数据源'], 1, '缓存是加速层，未命中时回源并建立缓存。'),
      q('为什么缓存必须有 TTL 或明确失效策略？', ['避免旧数据无限存在', 'TTL 会自动修复数据库', 'Redis 不允许永久键'], 0, '失效策略限制数据陈旧窗口。'),
      q('限流计数为什么需要原子操作？', ['避免并发读改写导致计数丢失或超额放行', '让计数显示为字符串', '减少网络地址长度'], 0, '并发请求必须以不可分割方式更新计数与窗口。')
    ],
    'backend-auth-testing': [
      q('认证与授权分别回答什么？', ['谁在请求；他能执行什么', '数据在哪；缓存多久', '请求多快；日志多大'], 0, '认证建立身份，授权基于身份和资源判断权限。'),
      q('用户密码应如何保存？', ['明文', '使用专用慢哈希并带盐', 'Base64 编码'], 1, '密码哈希需要抗暴力破解，Base64 不是加密。'),
      q('哪类测试最适合验证真实数据库事务与 HTTP 鉴权组合？', ['只测纯函数的单元测试', '集成测试', '静态截图'], 1, '集成测试跨越真实边界，验证组件协作风险。')
    ],
    'job-backend-nest-concepts': [
      q('把 NestJS Demo 从 `tsc` 改成只做语法转译后，构造函数中的 ChatService 无法解析。最应先检查什么？', ['是否仍生成 decorator metadata、入口是否加载 reflect-metadata，或是否需要显式 @Inject Token', '是否把 ChatService 改成 Request Scope', '是否把依赖创建移回 Controller 的 `new`'], 0, 'Nest 的构造注入依赖运行时 Token/元数据；只做语法转译可能不生成 design:paramtypes，接口等擦除类型还需显式 Token。'),
      q('请求带有效会话，但 body 的 message 不是字符串。职责划分最合理的是哪一项？', ['Guard 建立身份/授权，Pipe 校验和转换 DTO，Controller 只调用用例', 'Pipe 完成登录并直接写数据库，Guard 生成响应', 'Controller 手工完成身份、DTO、事务和模型调用'], 0, 'Guard、Pipe 与 Controller 分别处理访问决策、参数边界和协议映射。'),
      q('ChatService 依赖一个 TypeScript `ModelProvider` 接口，但接口运行时被擦除。Module 应如何装配？', ['用 Symbol/字符串 Token 注册实现，并在构造参数显式 @Inject(Token)', '直接把接口名放进 providers，容器会恢复接口', '把具体供应商实例保存到全局变量供 Service 读取'], 0, '接口没有运行时值，必须使用稳定 Token 把抽象与实现绑定。')
    ],
    'job-llm-concepts': [
      q('同一模型的长提示 TTFT 明显增加，但开始输出后的 tokens/s 接近短提示。最合理的解释是什么？', ['长输入增加 prefill 工作；decode 仍逐 Token 进行，KV cache 只避免重复计算已有位置', 'temperature 自动扩大了模型参数量', '流式 HTTP 把长提示重新训练进模型'], 0, 'Prefill 处理输入并构建 KV cache，主要影响首 Token；decode 的顺序生成决定后续速率。'),
      q('工单分类要求输出稳定 JSON，哪组设置与控制最合理？', ['较低随机性、Schema 约束、运行时校验和固定回归集', '提高 temperature，并从自由文本中截取大括号', '无限追加聊天历史，让模型自行记住格式'], 0, '采样设置只能降低波动，结构边界和正确性仍需 Schema、校验与评测。'),
      q('产品政策每天更新，回答必须带可核对出处。优先选择哪种方案？', ['运行时检索最新授权文档并返回引用，缺证据时拒答', '只微调回答语气并假设权重包含最新政策', '提高 top-p 让模型生成更多可能答案'], 0, '这属于动态外部事实与证据需求，RAG/业务 API 比行为微调更匹配。')
    ],
    'job-rag-concepts': [
      q('向量检索命中了另一个租户的高相似文档。正确的修复位置是什么？', ['在检索候选阶段按可信 tenant/permission 元数据过滤，并在返回引用前再次校验', '先把文档交给模型，再要求模型不要泄露', '只在前端隐藏引用，但允许正文进入上下文'], 0, '未授权文本不能进入模型上下文；权限过滤必须是服务端确定性边界。'),
      q('某问题有两个 relevant chunks，Top-2 只召回其中一个。该样本 Recall@2 是多少？', ['0.5，因为命中 1/2 个相关块', '1，因为至少命中了一个', '0，因为没有同时命中两个'], 0, 'Recall@K 是召回的相关项数量除以全部相关项数量，不能与 HitRate@K 混淆。'),
      q('Trace 显示正确政策 Chunk 已进入最终上下文，但答案给出相反结论。应优先归为什么？', ['生成忠实度/提示约束失败', '摄取阶段完全没有文档', '向量索引连接失败'], 0, '证据已被召回并进入上下文，故障重点转到生成和证据约束。')
    ],
    'job-agent-concepts': [
      q('退款流程步骤固定，只有用户意图分类较模糊。最合适的设计是什么？', ['确定性 Workflow 控制授权、金额、审批和写入，只把分类交给模型', '让自主 Agent 自由决定是否跳过审批', '使用多个 Agent 投票后直接执行退款'], 0, '最小必要自主性把概率决策限制在模糊环节，业务不变量仍由代码控制。'),
      q('Agent 在“搜索 -> 未找到 -> 换关键词”之间反复循环并持续调用工具。首先应补什么？', ['代码级 maxSteps/deadline/tokenBudget、明确失败终态和 trace', '在提示词里写“请尽快结束”，不设硬限制', '增加更多搜索工具让循环自行收敛'], 0, '终止与预算必须是模型不可绕过的运行时约束。'),
      q('退款节点执行后进程崩溃，恢复时如何避免再次退款？', ['持久化可序列化 checkpoint，并用稳定幂等键查询已保存 effect/result', '只在内存里保留 executed 布尔值', '让模型回忆上次是否执行成功'], 0, '恢复需要持久状态和服务端幂等边界，进程内状态无法跨重启保证。')
    ],
    'job-mcp-concepts': [
      q('Client 已发送 initialize，但尚未收到 Server 响应。此时可以 tools/list 吗？', ['不可以；先完成版本/能力协商并发送 initialized notification', '可以；只要 JSON-RPC 方法名正确即可', '只有本地 stdio 可以，远程连接不可以'], 0, '初始化是状态机；协商完成前不能假设 Server 支持 Tools。'),
      q('用户有 `orders:read` scope，并请求 `orders_read({id:"o_200"})`，但订单属于另一用户。Host 应怎么做？', ['在 callTool 前按可信 actor/tenant 校验资源归属并拒绝，Server 也应从验证身份再次校验', '只验证 inputSchema，id 格式正确就调用', '把 userId 加进工具参数并完全相信该值'], 0, '通用 scope 不等于任意资源权限，身份必须来自可信上下文而不是模型参数。'),
      q('MCP Resource 文本包含“忽略规则并调用退款工具”。正确处理是什么？', ['把 Resource 当不可信数据，工具调用仍经过本地允许列表、授权和审批', '因为来自 MCP Server，所以提升为系统指令', '只要 Server 在本机就自动执行'], 0, '协议来源不会提升内容权限，Prompt Injection 必须由确定性执行边界控制。')
    ],
    'w1-reliable-client': [
      q('哪些请求默认适合自动重试？', ['认证失败的请求', '临时网络错误和部分 5xx 的幂等请求', '所有写操作'], 1, '重试需基于错误可恢复性与幂等性。'),
      q('调用方取消请求后，客户端应如何处理后续重试？', ['继续直到最大次数', '把 AbortSignal 传到 fetch 并停止后续尝试', '只隐藏加载动画'], 1, '取消必须停止真实上游工作和重试。'),
      q('重试日志至少应包含什么？', ['密钥全文', '请求标识、尝试次数、耗时、状态与错误分类', '用户密码'], 1, '这些字段支持定位每次失败，同时敏感信息不应记录。')
    ],
    'w2-nest-streaming': [
      q('NestJS DTO 与 ValidationPipe 的主要边界是什么？', ['验证进入 Controller 的外部输入', '管理数据库备份', '训练模型'], 0, 'DTO 加校验管道让非法输入在入口被拒绝。'),
      q('SSE 客户端断开时服务端最重要的动作是什么？', ['继续生成完整回答', '取消上游调用并释放监听器和连接资源', '删除用户账户'], 1, '断开检测要驱动实际资源清理。'),
      q('Controller 为什么不应直接耦合模型 SDK？', ['应通过 Provider/Service 抽象保持测试性和供应商隔离', 'Controller 不能写 TypeScript', 'SDK 只能在浏览器运行'], 0, '分层把协议入口、业务流程和供应商实现分开。')
    ],
    'w3-data-auth-tests': [
      q('创建会话并写入首条消息时，事务失败应得到什么结果？', ['保留会话但丢失消息', '两项都回滚，不留下半成品', '随机保留一项'], 1, '同一业务不变量内的写入应共同提交或回滚。'),
      q('检查用户是否能读取某条消息时，不能只验证什么？', ['资源所有权或角色', 'JWT 是否有效', '服务端授权策略'], 1, '有效身份不代表有权访问任意资源，还必须检查授权与所有权。'),
      q('核心服务测试为什么必须覆盖失败路径？', ['故障、越权和回滚通常是高风险行为', '失败路径不会发生', '只为增加覆盖率数字'], 0, '可靠性来自对错误边界的可执行验证。')
    ],
    'w4-python-docker-e2e': [
      q('Node.js 与 Python 服务之间最稳定的边界是什么？', ['共享进程内变量', '版本化 HTTP 契约与明确超时错误', '互相读取源码文件'], 1, '跨语言协作应依赖可验证协议，不依赖实现细节。'),
      q('容器健康检查应该验证什么？', ['仅验证进程名称存在', '服务能否处理最低限度的真实健康请求', '镜像文件大小为偶数'], 1, '健康检查用于判断实例是否可接收流量。'),
      q('E2E 测试最适合验证什么？', ['单个纯函数分支', '从前端操作到多服务响应的关键用户闭环', '类型别名是否拼写正确'], 1, 'E2E 覆盖系统组装后的真实关键路径。')
    ],
    'w5-model-streaming': [
      q('模型 API Key 应存放在哪里？', ['浏览器打包代码', '服务端环境变量或密钥系统', '聊天消息里'], 1, '浏览器无法保守秘密，密钥必须留在受控服务端。'),
      q('Provider Adapter 的核心价值是什么？', ['让业务层不依赖供应商请求和响应字段', '让所有模型输出完全相同', '取消错误处理'], 0, '适配器归一化供应商差异，业务层依赖统一契约。'),
      q('流式响应中断后 UI 应进入什么状态？', ['永远保持加载中', '明确的取消或失败状态，并允许恢复操作', '假装回答完成'], 1, '流生命周期需要开始、增量、完成、取消和失败等明确状态。')
    ],
    'w6-structured-tools': [
      q('结构化输出为什么不能依赖从自然语言中截取 JSON？', ['格式可能不稳定，应由 schema 约束并验证', 'JSON 只能在数据库使用', '字符串截取速度太快'], 0, 'schema 提供机器可验证的结构边界。'),
      q('模型请求调用退款工具后，谁应做最终执行决定？', ['模型直接执行', '服务端校验参数、权限、幂等和审批后执行', '浏览器 LocalStorage'], 1, '模型只能提出意图，受信任服务端掌握执行权。'),
      q('订单查询工具收到 orderId 后仍需检查什么？', ['订单是否属于当前用户或其授权范围', 'orderId 字符数是否好看', '模型温度'], 0, '参数合法不等于资源访问被授权。')
    ],
    'w7-context-evals': [
      q('上下文窗口接近上限时，最合理的策略是什么？', ['无差别保留全部历史', '按任务保留相关证据，摘要或裁剪低价值内容', '随机删除系统指令'], 1, '上下文是有限信息预算，应优先保留影响任务的内容。'),
      q('可重复的评测基线至少需要什么？', ['固定样本、预期断言和版本化配置', '一次人工感觉', '只记录平均字数'], 0, '固定输入与断言让不同版本可以比较。'),
      q('模型路由决策应同时观察哪些维度？', ['质量、延迟和成本', '只看模型名称', '只看输出颜色'], 0, '生产路由需要在任务成功率、响应时间和成本间权衡。')
    ],
    'w8-rag-ingestion': [
      q('每个文档 chunk 至少应保留哪些溯源信息？', ['来源、标题、位置、版本和权限元数据', '只有向量', '只有随机编号'], 0, '元数据支持引用、更新、过滤和问题定位。'),
      q('为什么摄取链路必须幂等？', ['重复运行不应产生重复 chunk 和脏索引', '幂等会自动提高模型智力', '为了省略文档解析'], 0, '可重复构建是知识库维护和故障恢复的基础。'),
      q('权限过滤应在什么时候生效？', ['只在前端隐藏引用', '检索阶段就限制候选文档，并在服务端再次校验', '生成完成后再删除敏感词'], 1, '未授权内容不应进入模型上下文。')
    ],
    'w9-rag-retrieval': [
      q('混合检索结合了什么信号？', ['词法匹配与向量语义相似度', 'CPU 与 GPU 温度', '用户头像与字体'], 0, '两类信号互补专有词精确匹配与语义表达。'),
      q('可靠引用应指向什么？', ['模型生成的任意链接', '实际支撑答案的原文位置', '知识库首页'], 1, '引用必须让用户核对具体证据。'),
      q('没有足够证据时系统应该怎么做？', ['编造最可能答案', '明确拒答、说明缺口或转人工', '重复同一句话'], 1, '证据约束优先于强行回答。')
    ],
    'w10-rag-evaluation': [
      q('Recall@K 主要评估 RAG 的哪一部分？', ['检索是否把相关文档召回前 K 个候选', '最终答案文风', 'Docker 启动速度'], 0, 'Recall@K 是检索层指标。'),
      q('答案忠实度低但检索命中正常，优先排查什么？', ['生成提示与模型是否偏离证据', '向量库是否完全没有文档', 'CSS 颜色'], 0, '分层评测用于区分检索问题与生成问题。'),
      q('失败样本为什么要进入回归集？', ['防止后续优化让同类问题再次出现', '增加随机性', '隐藏失败记录'], 0, '回归集把真实故障转为长期门禁。')
    ],
    'w11-workflows-agents': [
      q('什么步骤最适合确定性 Workflow 而不是让模型自由决定？', ['权限检查和退款执行边界', '开放式内容草拟', '用户意图语言理解'], 0, '安全和业务不变量应由确定性代码控制。'),
      q('Agent 循环为什么必须有终止条件和上限？', ['避免无限调用、成本失控和不可恢复状态', '让模型回答更长', '为了关闭日志'], 0, '自主循环必须受步数、预算和状态约束。'),
      q('状态图的状态为什么应可序列化？', ['支持持久化、恢复和审计', '让变量名更短', '避免定义 schema'], 0, '可序列化状态是 checkpoint 和可恢复执行的基础。')
    ],
    'w12-persistence-hitl': [
      q('Checkpoint 应保存什么？', ['可恢复执行所需的业务状态与当前位置', '所有进程内对象和连接', '用户密码明文'], 0, '恢复需要稳定、可序列化的最小状态。'),
      q('人工重复点击“确认退款”时如何避免重复扣款？', ['隐藏按钮即可', '使用幂等键并持久化执行结果', '依赖模型记忆'], 1, '幂等控制必须位于服务端写入边界。'),
      q('审批链路为什么需要审批前后审计记录？', ['证明谁在何时批准了什么参数及最终结果', '让页面更热闹', '替代权限检查'], 0, '审计支持责任追踪和事故复盘，但不能替代授权。')
    ],
    'w13-mcp-security': [
      q('MCP 标准化了什么？', ['工具、资源和提示的发现与调用协议', '业务授权策略本身', '数据库事务隔离级别'], 0, 'MCP 提供互操作协议，不定义应用业务权限。'),
      q('接入 MCP 工具后还必须保留什么？', ['服务端鉴权、参数校验、最小权限和审计', '只保留工具描述', '允许模型调用所有写工具'], 0, '协议接入不能绕过原有安全边界。'),
      q('为什么仅靠系统提示不能防住 Prompt Injection？', ['不可信内容可能操纵模型，执行权仍需确定性策略控制', '提示词不能包含中文', '模型不会读取文档'], 0, '安全控制必须位于工具许可和执行层，而不是只依赖模型服从。')
    ],
    'w14-system-design': [
      q('智能体系统设计的第一步应该是什么？', ['先选择最热门框架', '明确用户任务、业务流程和失败模式', '先创建十个 Agent'], 1, '架构选择应由真实任务和风险驱动。'),
      q('工具权限矩阵应回答什么？', ['不同角色可以对哪些资源执行哪些操作', '模型输出字数', '前端主题颜色'], 0, '权限矩阵把主体、资源和操作边界明确化。'),
      q('什么样的成功指标可以进入验收？', ['可以从日志或评测稳定采集并有目标值', '“感觉很智能”', '只在演示时口头说明'], 0, '指标必须可测量、可重复和可追溯。')
    ],
    'w15-observability-deploy': [
      q('一次用户任务的 Trace 应连接哪些步骤？', ['入口、模型、检索、工具、数据库和最终响应', '只记录首页加载', '只记录成功请求'], 0, '端到端 Trace 用于定位跨组件的延迟和错误。'),
      q('部署门禁至少应包含什么？', ['健康检查、关键回归和可执行回滚方案', '只要构建成功', '删除旧日志'], 0, '能启动不等于能安全接流量，失败时还必须可回退。'),
      q('上游模型故障时，用户界面应如何表现？', ['无限加载', '给出可理解错误、重试或降级状态', '显示服务端堆栈和密钥'], 1, '错误反馈既要可操作，也不能泄露敏感内部信息。')
    ],
    'w16-evaluate-portfolio': [
      q('发布版本为什么应通过固定评测门禁？', ['让质量、延迟、成本和安全结论可重复', '保证永远没有任何错误', '替代所有人工判断'], 0, '固定门禁提供一致的发布依据，但仍需结合风险判断。'),
      q('简历中性能提升数字应满足什么条件？', ['来自可复现测试并能说明基线和方法', '估算一个更好看的数字', '只写百分比不写场景'], 0, '可追溯指标才能经得起面试和复现。'),
      q('为什么能够解释“不使用多 Agent”也是能力？', ['说明能根据复杂度、成本和可靠性选择最简单有效方案', '说明完全不懂 Agent', '因为多 Agent 永远不可用'], 0, '工程判断包括知道何时不增加不必要的自主性和协调成本。')
    ]
  };

  const missing = [];
  const answerOrders = [
    [0, 1, 2], [1, 2, 0], [2, 0, 1],
    [0, 2, 1], [2, 1, 0], [1, 0, 2]
  ];
  let lessonSequence = 0;
  for (const module of window.COURSE.modules) {
    for (const lesson of module.lessons) {
      const questions = quizzes[lesson.id];
      if (!questions) {
        missing.push(lesson.id);
        continue;
      }
      const answerOrder = answerOrders[lessonSequence % answerOrders.length];
      lessonSequence += 1;
      lesson.quiz = {
        passingScore: 100,
        questions: questions.map((question, index) => {
          const desiredAnswer = answerOrder[index];
          const correctOption = question.options[question.answer];
          const distractors = question.options.filter((_option, optionIndex) => optionIndex !== question.answer);
          const options = [];
          let distractorIndex = 0;
          for (let optionIndex = 0; optionIndex < question.options.length; optionIndex += 1) {
            options.push(optionIndex === desiredAnswer ? correctOption : distractors[distractorIndex++]);
          }
          return {
            ...question,
            id: `${lesson.id}-q${index + 1}`,
            options,
            answer: desiredAnswer
          };
        })
      };
    }
  }

  if (missing.length) throw new Error(`缺少章节测验：${missing.join(', ')}`);
})();
